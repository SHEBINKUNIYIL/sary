import Foundation

enum UIType: String, Codable {
    case grid = "grid"
    case linear = "linear"
    case slider = "slider"
}

enum DataTypes: String, Codable {
    case smart = "smart"
    case group = "group"
    case banner = "banner"
    case sku = "sku"

    
}
